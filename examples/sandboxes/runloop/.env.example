# Runloop — provisions the devbox that runs the BrowseCLI.
# Get one at https://platform.runloop.ai
RUNLOOP_API_KEY=

# Browserbase — the Verified browser the devbox reaches over CDP.
# Get these at https://www.browserbase.com (Settings → API).
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
